# requirements_tf.txt
# Core for TensorFlow-based projects (training, inference, analysis, ONNX export)
# Install: 
#   1. conda create -n tf python=3.10; conda activate tf; 
#   2. pip install -r requirements_tf.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# GPU note: TF bundles specific CUDA (e.g., 2.10 = CUDA 11.2; newer = CUDA 12.x). No flexible 11/12 switch like PyTorch.
# On Windows native GPU: Stick to <=2.10; for CUDA 12.x use Linux or WSL2.

# 0.
tensorflow>=2.10.0,<2.16.0  # Stable range for legacy migration; includes Keras
tensorflow-addons>=0.20.0,<1.0.0
tqdm>=4.60.0,<5.0.0         # Progress & basics
h5py>=3.0.0,<3.12.0         # Model weights save/load

# 1. ONNX export
onnx>=1.14.0,<1.17.0
tf2onnx>=1.15.0,<2.0.0      # Reliable TF to ONNX converter

# 2. Visualization
matplotlib>=3.5.0,<3.10.0   # Plot results, training curves

# Scientific base
numpy>=1.21.0,<2.0.0        # Safe zone for TF 2.10~2.15 (avoid NumPy 2.0 breaks)
scipy>=1.7.0,<1.12.0        # Filters

# 4. FLOPs / MACs analysis
#onnx-tool>=0.7.0,<1.0.0     # ONNX model inspection



# requirements_tf_vision.txt
# Vision-specific extras (image processing, metrics, etc.)
# pip install -r requirements_tf_vision.txt  (after requirements_tf.txt)

# 1. Image Processing
opencv-python>=4.5.0,<5.0.0
Pillow>=9.5.0,<11.0.0
scikit-image>=0.19.0,<0.23.0  # SSIM, PSNR, common filters
